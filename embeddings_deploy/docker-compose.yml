services:
  vllm:
    build: .
    container_name: vllm_embed
    ports:
      - "8000:8000"  # Ajusta el puerto según necesidad
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: always
